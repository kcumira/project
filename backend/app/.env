# 資料庫連線設定
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
# 這裡填寫你在 docker-compose.yml 裡設定的 MYSQL_ROOT_PASSWORD
DB_PASSWORD=root
# 這裡填寫你在 TablePlus 裡建立的資料庫名稱
DB_NAME=smartfood

# 如果你的專案還有其他設定（例如 API 金鑰），也可以放在這裡
GEMINI_API_KEY=AIzaSyCGz4679MaRqHhEoTXvfdkzy-7yeXIEot8

/**
python3 -m venv venv
source venv/bin/activate
pip install fastapi uvicorn mysql-connector-python python-dotenv
pip install bcrypt
pip install cryptography
pip install requests
pip install google-generativeai
pip install python-multipart
uvicorn app.main:app --reload
*/